---
trigger: model_decision
description: When making design choices about ActiveAgent and Rails Controllers
---

The agent must favor simple, maintainable patterns over complex abstractions. It must treat ActiveAgent classes strictly as standard Rails controllers and construct the user interface strictly using standard ERB templates.
